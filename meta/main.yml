dependencies:
  - role: jumperfly.ansible_common
  - role: jumperfly.cfssl
  - role: jumperfly.kubelet
    when: etcd_pod_install

galaxy_info:
  role_name: etcd_node
  author: jumperfly
  description: ansible-role-etcd-node
  license: Apache
  min_ansible_version: "2.0"
  platforms:
    - name: EL
      versions:
        - "7"
